package nmsu.hcc.pattern_triggers.listeners;

import android.graphics.Bitmap;

import java.io.File;
import java.io.FileNotFoundException;

public interface LatestBitmapImageListener {
    void latestBitmapImage(Bitmap imageBitmap);
}
